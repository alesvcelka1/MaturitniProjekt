rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // DOČASNÁ PRAVIDLA PRO TESTOVÁNÍ - VŠICHNI PŘIHLÁŠENÍ MOHOU ČÍT/PSÁT
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}